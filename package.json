{
  "name": "gemini-playground",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "npx wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "deno": "deno run --allow-net --allow-read --allow-env src/deno_index.ts"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.2",
    "vitest": "2.1.8",
    "wrangler": "^3.60.3"
  },
  "dependencies": {
    "node-record-lpcm16": "^1.0.1"
  }
}
